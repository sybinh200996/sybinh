NAM30 STABLE UX LOCKDOWN

Base: NAM26 stable.
Goal: keep features, fix responsive safely.

Changes:
- Desktop and mobile hero/banner balanced without poster effect.
- Function tabs centered, no right drift.
- Feature tabs use grid instead of awkward horizontal layout on mobile.
- Desktop dashboard cards balanced in 2 columns.
- Bottom nav less intrusive and app gets bottom padding.
- Mobile quick buttons reduced safely.
- Keeps NAM26 features: Tình duyên, Thần số học, stats, voice controls, Multi-AI.

Deploy:
1) Extract ZIP into project folder.
2) git add .
3) git commit -m "NAM30 stable UX lockdown"
4) git pull origin main --rebase
5) git push origin main
