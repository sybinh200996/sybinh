NAM37 - Careful UI Weather Banner Tabs Fix

Đã sửa cẩn thận trên nền NAM36:
1. Sửa lỗi server.js bị khai báo trùng directWeatherAnswer khiến Node không chạy.
2. Khóa lại banner: ảnh phải dùng đúng file assets/hero-right.jpg, object-fit contain, vừa khung banner, viền trái được mask mờ hòa nền.
3. Căn lại ảnh trái/phải và vùng chữ để không đè nút/ô tìm kiếm.
4. Khóa thanh tab chức năng một hàng, không lệch, không bung 2-3 dòng; cuộn ngang mượt khi màn hình hẹp.
5. Giảm chiều cao bottom nav để không che dashboard/bảng cuối.
6. Giữ lõi thời tiết thật: ưu tiên Open-Meteo theo tọa độ, fallback wttr.in, không cho AI trả lời lạc địa danh.

Cách chạy:
npm install
npm start
Mở: http://localhost:3000
