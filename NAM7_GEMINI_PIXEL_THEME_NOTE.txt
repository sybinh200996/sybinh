NAM7 - Gemini AI thật + UI pixel theme

Đã nâng cấp:
1. Fix chatbot Gemini: không dùng câu trả lời mẫu khi API lỗi.
2. Thêm thời gian hiện tại Việt Nam vào prompt server để tránh lỗi xuyên không năm 2024.
3. Thêm trạng thái Gemini API trên giao diện.
4. Fix payload xem chỉ tay/xem tướng để frontend gửi đúng backend.
5. Fix payload tình duyên/tử vi để backend nhận dữ liệu từ form hiện tại.
6. Giữ giao diện cosmic neon giống ảnh mẫu: banner 60%, tab thu 80%, ảnh banner chỉ fit/crop, không thêm bớt.

Cách chạy:
- npm install
- copy .env.example thành .env
- thêm GEMINI_API_KEY=...
- npm start
- mở http://localhost:3000
