NAM34 - WEATHER SERVER GUARD + BANNER ORIGINAL FIT + COMPACT TABS

Đã sửa:
1. AI Chat bắt câu hỏi thời tiết ở frontend trước, gọi /api/weather riêng rồi mới fallback sang AI.
2. Server thêm route /api/weather và vẫn giữ chặn thời tiết trong /api/multi-ai/chat + /api/chat-ai.
3. Việt Trì / Phú Thọ map tọa độ cố định, tránh nhầm địa danh.
4. Nếu hosting chặn API thời tiết, app vẫn báo đúng địa điểm và nguyên nhân, không trả lời lạc đề.
5. Ảnh banner phải giữ ảnh gốc, object-fit contain, vừa banner, không blend méo ảnh.
6. Dãy tab phía trên chuyển thành thanh cuộn ngang gọn, không sổ hết ra màn hình.

Sau khi upload lên host, nên xóa cache trình duyệt hoặc mở ẩn danh để thấy CSS/JS mới.
